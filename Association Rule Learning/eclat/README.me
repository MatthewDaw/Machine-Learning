#Machine Learning
Eclait algorithm used to determine what items customers will likely buy with other items.

This algorithm acts effectively the exact same way as Apriori except it only outputs the lift.